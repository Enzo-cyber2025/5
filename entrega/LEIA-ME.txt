GGUF-Chat-repaired.apk — build local de teste atualizado, 13/09/2026

Assinatura v2/v3 verificada. Novos reparos: seletor interno de modelos, guarda de
Enviar, acesso privado dos workers e dependência libdl da ponte JNI.

A execução CI 34769745227 comprovou duas gerações nativas em Android 11 x86_64.
Porém, as respostas foram inadequadas aos pedidos. Português/UTF-8, importação SAF,
GPU, visão e funcionamento no aparelho alvo NÃO estão aprovados.
O teste Vulkan 34771124245 reprovou: backend retornou NULL e usou CPU (offload=0).
O emulador usa SwiftShader por software, não GPU física.
Veja ../docs/VULKAN.md, ../docs/GERACAO.md e ../docs/VALIDACAO.md.

Este APK foi reconstruído localmente, com a mesma chave local da entrega anterior.
NÃO é o binário exato executado no CI: o CI usa assinatura descartável e o DEX dos
builds local/remoto tem hashes diferentes; essa diferença ainda não foi investigada.
As bibliotecas nativas locais correspondem byte a byte às do último teste CI.

A chave continua diferente da assinatura do aplicativo original. Não desinstale
sua versão sem salvar conversas/modelos importantes: desinstalar apaga dados.
Use um aparelho/emulador separado se não conseguir fazer backup.
