name: Small projector gains combined quality and regression qualification
on:
  push:
    branches: [arena/01a09b42-5]
    paths: [.github/workflows/projector-combined.yml, scripts/test_projector_combined_android.py, ci/evaluate_projector_combined.py, tests/test_projector_combined.py]
permissions:
  contents: write
  actions: read
concurrency:
  group: projector-combined-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    if: ${{ contains(github.event.head_commit.message, '[projector combined]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 85
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Use the Android SDK already installed on the hosted runner
        run: |
          test -d "$ANDROID_HOME"
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          test -x "$SDKMANAGER"
          echo "$(dirname "$SDKMANAGER")" >> "$GITHUB_PATH"
          # Avoid the failing setup action; accept licenses on the existing SDK.
          set +o pipefail
          yes | "$SDKMANAGER" --licenses > /tmp/android-licenses.log
          set -o pipefail
      - name: Pinned tools and source APK
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 -m venv .venv
          .venv/bin/pip install -r requirements-test.txt
          bash scripts/fetch_tools.sh
          python3 scripts/fetch_original.py
          sdkmanager 'ndk;28.2.13676358' 'platforms;android-35' 'build-tools;35.0.0'
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/28.2.13676358" >> "$GITHUB_ENV"
          sudo apt-get update -qq
          sudo apt-get install -y ninja-build glslc libvulkan-dev
          git clone --depth 1 --branch vulkan-sdk-1.4.357.0 https://github.com/KhronosGroup/Vulkan-Headers .cache/vulkan-sdk
          test "$(git -C .cache/vulkan-sdk rev-parse HEAD)" = e3b1eec08173d6b825cd3ac88c885a63b621504a
          git clone --depth 1 --branch vulkan-sdk-1.4.357.0 https://github.com/KhronosGroup/SPIRV-Headers .cache/spirv-headers
          test "$(git -C .cache/spirv-headers rev-parse HEAD)" = 29981f65241605e08b0ede4cfeb999fe3b723c6a
          cmake -S .cache/spirv-headers -B .cache/spirv-build -DCMAKE_INSTALL_PREFIX="$PWD/.cache/spirv-install"
          cmake --install .cache/spirv-build
          mkdir -p .cache/vulkan-headers
          cp -r .cache/vulkan-sdk/include/. .cache/vulkan-headers/
      - uses: actions/cache@v4
        id: native-cache
        with:
          path: |
            .cache/llama-mobile
            .cache/mobile/arm64-v8a
            .cache/mobile/x86_64
          key: native-v041-ndk28-v1-${{ runner.os }}-${{ github.run_id }}
          restore-keys: native-v041-ndk28-v1-${{ runner.os }}-
      - name: Compile real native stack and compact UI
        env:
          APKTOOL_JAR: .cache/tools/package/lib/apktool.jar
          GGUF_REUSE_TESTED_NATIVE: '0'
          GGUF_EXPERIMENT_BLOCKING_WAIT: '0'
        run: bash ci/log_step.sh mobile-build python3 apk-fix/build_mobile.py
      - name: Save compiled native objects before Android acceptance (including on later test failure)
        if: steps.native-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            .cache/llama-mobile
            .cache/mobile/arm64-v8a
            .cache/mobile/x86_64
          key: native-v041-ndk28-v1-${{ runner.os }}-${{ github.run_id }}
      - name: Real strict dispatch and exact combination/control checks
        run: |
          bash ci/log_step.sh strict-host bash scripts/test_strict_vulkan_host.sh
          .venv/bin/python -m pytest -q tests/test_projector_combined.py tests/test_projector_batch.py tests/test_projector_qkv.py tests/test_strict_vulkan.py tests/test_projector_cache.py tests/test_latency.py
      - name: Sign one disposable comparison APK, never replace the delivered file
        run: |
          mkdir -p .cache/combined-export evidence
          keytool -genkeypair -keystore "$RUNNER_TEMP/combined-test.p12" -storetype PKCS12 -storepass android -keypass android -alias test -keyalg RSA -keysize 2048 -validity 2 -dname 'CN=Disposable projector combination experiment NOT release'
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" sign --ks "$RUNNER_TEMP/combined-test.p12" --ks-pass pass:android --out .cache/combined-export/candidate.apk dist/GGUF-Chat-mobile-unsigned.apk
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose .cache/combined-export/candidate.apk
          "$ANDROID_HOME/build-tools/35.0.0/zipalign" -c -P 16 4 .cache/combined-export/candidate.apk
          rm "$RUNNER_TEMP/combined-test.p12"
          python3 - <<'PY'
          import hashlib,json,sys,subprocess,zipfile
          from pathlib import Path
          sys.path.insert(0,'scripts')
          from sign_gain_release import payload
          apk=Path('.cache/combined-export/candidate.apk')
          assert payload(apk)==payload('dist/GGUF-Chat-mobile-unsigned.apk')
          with zipfile.ZipFile(apk) as z:
              for abi in ('x86_64','arm64-v8a'):
                  assert b'GGUF_PROJECTOR_COMBINATION enabled=' in z.read('lib/'+abi+'/libaijni.so')
                  assert b'GGUF_VULKAN_WAIT_POLICY' not in z.read('lib/'+abi+'/libaijni.so')
          r=dict(apk_sha256=hashlib.sha256(apk.read_bytes()).hexdigest(),source_commit=subprocess.check_output(['git','rev-parse','HEAD'],text=True).strip(),default_combined=False,control='Same APK; stock RGB in both modes; before no experiments, after explicit batch2+QKV+combination flags',release_approved=False)
          Path('.cache/combined-export/build.json').write_text(json.dumps(r,indent=2))
          Path('evidence/physical-combined-build.json').write_text(json.dumps(r,indent=2))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: combined-payload-not-release
          path: .cache/combined-export/*
          include-hidden-files: true
          retention-days: 7
      - name: Publish build diagnostics
        if: always()
        env:
          GGUF_EVIDENCE_VARIANT: combined-build
        run: bash ci/publish_evidence.sh
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: combined-build-diagnostics
          path: |
            evidence/
            .cache/ci-logs/
  verify:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: actions/download-artifact@v4
        with:
          name: combined-payload-not-release
          path: .cache/combined-import
      - name: Exact fixtures and emulator prerequisites
        run: |
          mkdir -p evidence
          python3 -m venv .venv
          .venv/bin/pip install -r requirements-test.txt numpy==2.2.6 tqdm==4.67.1 pyyaml==6.0.2
          git clone --depth 1 --branch v0.4.1 https://github.com/ggml-org/llama.cpp .cache/llama-mobile
          test "$(git -C .cache/llama-mobile rev-parse HEAD)" = b29c606e28a01b1bc8c1351026a0fa6e616bf6c4
          bash ci/mobile-models.sh
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" 'platforms;android-35' 'build-tools;35.0.0'
          bash scripts/build_test_input.sh
          bash scripts/build_code_test.sh .cache/combined-import/candidate.apk
          sudo chmod a+rw /dev/kvm
          bash ci/prepare_vulkan.sh
      - uses: reactivecircus/android-emulator-runner@v2
        env:
          GH_TOKEN: ${{ github.token }}
          GGUF_MOBILE_VULKAN: '1'
          GGUF_TEST_APK: .cache/combined-import/candidate.apk
          GGUF_TEST_STRICT_GPU: '1'
        with:
          api-level: 35
          arch: x86_64
          target: google_apis
          profile: pixel_5
          ram-size: 6144M
          heap-size: 1024M
          disk-size: 8192M
          disable-animations: true
          emulator-options: -no-window -no-snapshot -gpu host -feature Vulkan -noaudio -no-boot-anim -camera-back emulated -camera-front emulated
          script: .venv/bin/python scripts/test_projector_combined_android.py verify
      - name: Require full original weight and embedding bytes before timing
        run: python3 ci/evaluate_projector_combined.py evidence/summary.json > evidence/physical-combined-verification.json
      - uses: actions/upload-artifact@v4
        with:
          name: combined-byte-proof
          path: evidence/summary.json
      - name: Publish verification, including a numerical mismatch
        if: always()
        env:
          GGUF_EVIDENCE_VARIANT: combined-verify
        run: bash ci/publish_evidence.sh
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: combined-verification-evidence
          path: evidence/
  measure:
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        kind: [awake, asleep, text, cache]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: actions/download-artifact@v4
        with:
          name: combined-payload-not-release
          path: .cache/combined-import
      - uses: actions/download-artifact@v4
        with:
          name: combined-byte-proof
          path: .cache/combined-proof
      - name: Exact fixtures and emulator prerequisites
        run: |
          mkdir -p evidence
          python3 -m venv .venv
          .venv/bin/pip install -r requirements-test.txt numpy==2.2.6 tqdm==4.67.1 pyyaml==6.0.2
          git clone --depth 1 --branch v0.4.1 https://github.com/ggml-org/llama.cpp .cache/llama-mobile
          test "$(git -C .cache/llama-mobile rev-parse HEAD)" = b29c606e28a01b1bc8c1351026a0fa6e616bf6c4
          bash ci/mobile-models.sh
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" 'platforms;android-35' 'build-tools;35.0.0'
          bash scripts/build_test_input.sh
          bash scripts/build_code_test.sh .cache/combined-import/candidate.apk
          sudo chmod a+rw /dev/kvm
          bash ci/prepare_vulkan.sh
      - uses: reactivecircus/android-emulator-runner@v2
        env:
          GH_TOKEN: ${{ github.token }}
          GGUF_MOBILE_VULKAN: '1'
          GGUF_TEST_APK: .cache/combined-import/candidate.apk
          GGUF_TEST_STRICT_GPU: '1'
        with:
          api-level: 35
          arch: x86_64
          target: google_apis
          profile: pixel_5
          ram-size: 6144M
          heap-size: 1024M
          disk-size: 8192M
          disable-animations: true
          emulator-options: -no-window -no-snapshot -gpu host -feature Vulkan -noaudio -no-boot-anim -camera-back emulated -camera-front emulated
          script: .venv/bin/python scripts/test_projector_combined_android.py ${{ matrix.kind }}
      - name: Independent small-gain and nonregression evaluation
        run: python3 ci/evaluate_projector_combined.py evidence/summary.json .cache/combined-proof/summary.json > evidence/physical-combined-gate.json
      - name: Publish bounded paired results, including non-approval
        if: always()
        env:
          GGUF_EVIDENCE_VARIANT: combined-${{ matrix.kind }}
        run: bash ci/publish_evidence.sh
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: combined-${{ matrix.kind }}-evidence
          path: evidence/
